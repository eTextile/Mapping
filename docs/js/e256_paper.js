let e256_blobs = new blobs();

let blobArray [] = e256_blobs.get();

for (let blob = 0;  blob < blobArray.length; blob++){
    console.log (blobArray[i].id);
} 

var myCircle = new Path.Circle(new Point(100, 70), 50);
myCircle.fillColor = 'black';